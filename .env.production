# Set this to the deployed Cloud Run URL of product-api before building the Docker image.
# This value is baked into the JavaScript bundle at build time via: docker build --build-arg VITE_API_URL=<value>
VITE_API_URL=https://YOUR_API_CLOUD_RUN_URL
