# VITE_API_URL is only used as a local dev fallback (read by Vite at build time).
# In Docker / Cloud Run, set the API_URL environment variable on the container instead —
# docker-entrypoint.sh writes it into /env-config.js at startup so no rebuild is needed.
VITE_API_URL=https://YOUR_API_CLOUD_RUN_URL
